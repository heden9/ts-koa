{
  "dependencies": {
    "@types/koa": "^2.0.44",
    "@types/koa-router": "^7.0.27",
    "koa": "^2.5.0",
    "koa-router": "^7.4.0"
  },
  "scripts": {
    "start": "rimraf dist && tsc && NODE_ENV=development node ./dist/app.js",
    "debug": "rimraf dist && tsc && node --inspect-brk ./dist/app.js",
    "dev": "nodemon"
  },
  "devDependencies": {
    "rimraf": "^2.6.2"
  }
}
